dependencies {
    testImplementation(libs.swissknife.ddd.domain)
    testImplementation(libs.swissknife.correlation.core.domain)
    testImplementation(libs.swissknife.logging.standard.slf4j.configuration)
    testImplementation(libs.swissknife.pulsar.json.serialization)
    testImplementation(libs.swissknife.pulsar.test.utils)
    testImplementation(libs.swissknife.ddd.test.utils)
    testImplementation(libs.swissknife.protected.value.factory.aes)
    testImplementation(libs.swissknife.cryptography.implementation.bouncy.castle)
    testImplementation(libs.swissknife.avro.schema.catalogue.test.utils)
    testImplementation(libs.swissknife.pulsar.avro.serialization)
    testImplementation(libs.swissknife.pulsar.avro.utils)
    testImplementation(libs.swissknife.pulsar.messaging.adapter)
    testImplementation(libs.swissknife.pulsar.messaging.test.utils)
    testImplementation(libs.pillar.avro.serialization.test.utils)
    testImplementation(libs.pillar.json.serialization.ddd)
    testImplementation(libs.pillar.messaging.test.utils)
    testImplementation(libs.pillar.messaging.conventions)
    testImplementation(libs.pillar.protected.value.domain)
    testImplementation(libs.pillar.avro.serialization.core)
    testImplementation(libs.pillar.avro.serialization.correlation.core)
    testImplementation(libs.pillar.avro.serialization.ddd)
    testImplementation(libs.pillar.protected.value.messaging.serialization.avro)
}