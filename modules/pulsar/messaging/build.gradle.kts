dependencies {
    testImplementation(projects.swissknifeDddDomain)
    testImplementation(projects.swissknifeCorrelationCoreDomain)
    testImplementation(projects.swissknifeLoggingStandardSlf4jConfiguration)
    testImplementation(projects.swissknifePulsarJsonSerialization)
    testImplementation(projects.swissknifePulsarTestUtils)
    testImplementation(projects.pillarMessagingTestUtils)
    testImplementation(projects.pillarJsonSerializationDdd)
    testImplementation(projects.swissknifeDddTestUtils)
    testImplementation(projects.pillarMessagingConventions)
    testImplementation(projects.pillarProtectedValueDomain)
    testImplementation(projects.swissknifeProtectedValueFactoryAes)
    testImplementation(projects.swissknifeCryptographyImplementationBouncyCastle)
    testImplementation(projects.swissknifeAvroSchemaCatalogueTestUtils)
    testImplementation(projects.swissknifePulsarAvroSerialization)
    testImplementation(projects.swissknifePulsarAvroUtils)
    testImplementation(projects.swissknifePulsarMessagingAdapter)
    testImplementation(projects.swissknifePulsarMessagingTestUtils)
    testImplementation(projects.pillarAvroSerializationTestUtils)
    testImplementation(projects.pillarAvroSerializationCore)
    testImplementation(projects.pillarAvroSerializationCorrelationCore)
    testImplementation(projects.pillarAvroSerializationDdd)
    testImplementation(projects.pillarProtectedValueMessagingSerializationAvro)
}